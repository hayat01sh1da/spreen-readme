# frozen_string_literal: true
# rbs_inline: enabled

# Convenience shim so that both `require 'spreen-readme'` (the gem name) and
# `require 'spreen_readme'` (the conventional path) work.
require_relative 'spreen_readme'
